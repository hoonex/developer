[package]
name = "aeroforge-app"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
aeroforge-accurate-backend = { path = "../accurate_backend" }
aeroforge-flow-core = { path = "../flow_core" }
aeroforge-geometry-core = { path = "../geometry_core" }
aeroforge-volume-core = { path = "../volume_core" }
bevy = { version = "0.19.1", features = ["mesh_picking"] }
bevy_egui = "0.40.1"
bevy_panorbit_camera = { version = "0.35.0", features = ["bevy_egui"] }
bytemuck = "1.23"
